# Agent Identity CLI platform binary

This package is installed automatically by `@authing/agent-identity-cli` for a
matching operating system and CPU architecture. Install the main package rather
than depending on this package directly.
